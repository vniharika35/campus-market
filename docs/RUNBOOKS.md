# Runbooks
## Local dev
- Prereqs: Node 20+, Docker (for Postgres)
- Start API (to be added), connect to DB via .env

## Environments
- Local now; staging/prod later

## Secrets
- Never commit tokens/keys; use .env locally; use cloud secret managers later
