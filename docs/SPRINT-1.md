# Sprint 1 (2 weeks)
Goal: API foundation — /healthz, local Postgres + schema, Auth skeleton, Listings CRUD; wireframes PNGs.

Stories (acceptance summarized):
- API scaffold + /healthz: Node project in /api; GET /healthz returns {status:"ok"}
- Local Postgres + schema v1: docker-compose; users, listings, listing_images; .env.example
- Auth skeleton: JWT; campus email domain check; Zod validation; /auth/login and /auth/me
- Listings CRUD: POST/GET/GET:id/PATCH/mark-sold; validation + pagination
- Wireframes: Home, Listing detail, Create listing (PNGs in docs/wireframes)
